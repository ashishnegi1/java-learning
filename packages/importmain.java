package packages;
import generics.*;

public class importmain {
    public static void main(String[] args) {
        wildcards example = new wildcards();// main method is not called implicitly
    }
}
